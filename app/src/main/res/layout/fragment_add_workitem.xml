<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:bind="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="taskr.se.taskr.home.workitemviewmodel.AddWorkItemViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="5dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/title"
            android:textStyle="bold" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/title"
            android:maxLength="15"
            android:text="@={viewModel.title}"
            app:error="@{viewModel.titleError}" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            android:text="@string/description"
            android:textStyle="bold" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/description"
            android:text="@={viewModel.description}"
            app:error="@{viewModel.descriptionError}"
            app:limiter="@{viewModel.description}" />

        <android.support.v7.widget.AppCompatSpinner
            android:id="@+id/item_status"
            android:layout_width="wrap_content"
            android:paddingBottom="5dp"
            android:paddingTop="5dp"
            android:layout_height="wrap_content"
            android:entries="@array/item_status"
            bind:selectedValue="@={viewModel.status}"
            app:setVisible="@{viewModel.visible}"
            />

        <Button
            android:id="@+id/save_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            android:onClick="@{() -> viewModel.save()}"
            android:text="@string/add_workitem"
            android:textColor="#FFF"
            android:textStyle="bold" />

    </LinearLayout>

</layout>